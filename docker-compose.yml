version: '3.7'

# ---------------------------------------------------------------------------- #

services:

  # -------------------------------------------------------------------------- #

  postgres:
    image: openbank/postgres:v0.0.0-main
    hostname: postgres
    restart: on-failure
    volumes:
      - openbank-secondary:/var/lib/postgresql/data

  # -------------------------------------------------------------------------- #

  metrics:
    image: openbank/metrics:v0.0.0-main
    hostname: metrics
    restart: on-failure
    volumes:
      - openbank-metrics:/opt/metrics/data

  # -------------------------------------------------------------------------- #

  demo:
    image: openbank/demo
    build:
      context: .
      dockerfile: Dockerfile
      args:
        LAKE_VERSION: 1.2.5
        VAULT_VERSION: 1.3.2
        LEDGER_VERSION: 1.1.2
        FIO_BCO_VERSION: 1.3.0
        BONDSTER_BCO_VERSION: 1.3.0
        DWH_VERSION: 1.0.0
    ports:
      - "443:443"
      - "8443:8443"
      - "4400:4400"
      - "4401:4401"
      - "4001:4001"
      - "4002:4002"
    links:
      - postgres
      - metrics
    volumes:
      - /sys/fs/cgroup:/sys/fs/cgroup:ro
      - openbank-primary:/data

# ---------------------------------------------------------------------------- #

volumes:
  openbank-primary:
  openbank-secondary:
  openbank-metrics:
    driver: local
    driver_opts:
      o: bind
      type: none
      device: ${PWD}/report/metrics

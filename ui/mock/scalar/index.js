
const DateTimeScalar = require("./DateTime")
const BigDecimalScalar = require("./BigDecimal")

/* -------------------------------------------------------------------------- */

module.exports = Object.freeze({
  DateTimeScalar,
  BigDecimalScalar,
})

/* -------------------------------------------------------------------------- */

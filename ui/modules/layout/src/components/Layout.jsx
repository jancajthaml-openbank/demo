import React from 'react';

import { Module } from '@lastui/rocker/platform';

const Layout = () => {
	return (
		<div>
			<Module name="header" />
			<Module name="content" />
		</div>
	);
}

export default Layout;